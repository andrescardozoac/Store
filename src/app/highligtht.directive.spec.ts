import { HighligthtDirective } from './highligtht.directive';

describe('HighligthtDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligthtDirective();
    expect(directive).toBeTruthy();
  });
});
